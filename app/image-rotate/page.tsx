import { RotateTool } from '@/components/tools/RotateTool';

export const metadata = {
  title: 'Rotate & Flip Image – Rotate Photos 90, 180, 270 Online Free',
  description:
    'Rotate and flip images online directly in your browser. Rotate 90 degrees, mirror horizontally or vertically in batch with zero server uploads.',
};

export default function RotatePage() {
  return <RotateTool />;
}
