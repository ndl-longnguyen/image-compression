import { EditorTool } from '@/components/tools/EditorTool';

export const metadata = {
  title: 'Image Editor & Watermark – Edit Photos & Add Watermarks Online Free',
  description:
    'Adjust brightness, contrast, apply artistic filters, or stamp custom text and logo watermarks directly in your browser. 100% private, zero uploads.',
};

export default function EditorPage() {
  return <EditorTool />;
}
